__pycache__/
*.pyc
.pytest_cache/

# worktrees now live nested inside this folder (moved by hand) —
# they are separate git working trees, not part of this repo's own files
loop-project-4-worktree-good/
loop-project-4-worktree-bad/
