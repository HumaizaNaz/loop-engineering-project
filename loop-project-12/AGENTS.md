# Dreaming loop — Project 12 capstone

Second capstone: a loop over a loop. Watches Project 8's real spine
(`progress.md`/`beat.log`) and proposes rule changes to Project 8's own
skill/agents files — evidence-cited only, never a guess, never a direct
commit. Follow the `dreaming-loop` skill exactly.

## Heartbeat — same honest gap as the rest of this build

A real weekly cron was not attempted — the same account-level Routines
restriction documented in Projects 6/9/10/11 applies. This build
simulates the heartbeat as a single, real, memory-isolated dispatched
beat over Project 8's real (and one deliberately planted) log history,
the same honest pattern as Project 8 itself used for its own heartbeat.

## Maker-checker

The dreaming loop (maker) drafts a proposal with cited evidence. A
separate, fresh checker independently re-reads the same logs and
verifies the citations are real — not invented, not stretched — before
any human considers merging.
